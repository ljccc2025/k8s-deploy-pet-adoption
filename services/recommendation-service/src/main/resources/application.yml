spring:
  application:
    name: recommendation-service

server:
  port: 8088

management:
  endpoints:
    web:
      exposure:
        include: health,info

pet-service:
  base-url: ${PET_SERVICE_BASE_URL:http://localhost:8083}
